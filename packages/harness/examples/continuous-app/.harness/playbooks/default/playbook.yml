name: default
description: Build a Todo app from scratch

run:
  mode: autonomous
  maxIterations: 50
  maxTaskAttempts: 3
  resume: true

checks:
  - id: builds-clean
    cmd: "npm run build"
  - id: tests-pass
    cmd: "npm test"
