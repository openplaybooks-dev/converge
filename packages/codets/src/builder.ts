/**
 * Backward-compatible alias — `CodeBuilder` is `ReactBuilder`.
 *
 * Existing code importing `CodeBuilder` from `codegen` continues
 * to work unchanged.
 */
export { ReactBuilder as CodeBuilder } from './react-builder.ts';
