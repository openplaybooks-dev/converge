name: fix-issue
description: Fix a GitHub issue end-to-end

inputs:
  issue:
    required: true
    description: Issue number

key: issue

run:
  mode: autonomous
  maxTaskAttempts: 3
  maxDuration: 30m
  resume: true
