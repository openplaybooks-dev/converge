{
  "epoch": "12",
  "selected": {
    "id": "hooks-throw-timeout",
    "priority_class": "correctness",
    "dimension": "Correctness",
    "goal": "Fix root-cause of hook error handling so throwing hooks do not block downstream task progression",
    "why_now": "Failing test is the highest-priority evidence: pnpm vitest run tests/playbook-hooks.test.ts times out at 10000ms on 'should handle hooks that throw without blocking downstream' (line 225). This is a priority-1 stalled-run root cause under the maintainer policy. All higher priority classes (1) are satisfied by this finding itself. Priorities 2–6 were checked: no state/lifecycle crash, no DAG non-determinism regression, no provider cleanup failures, no API contract drift, and no pending doc-only gap. This is not a repeat of epochs 5 (runstate-missing-crash) or 11 (deprecation-warning-spam) — different failure class (promise/lock lifecycle in hook path vs. runstate deserialization vs. logger dedup).",
    "files": ["packages/core/src/run.ts", "tests/playbook-hooks.test.ts"],
    "test_command": "pnpm vitest run tests/playbook-hooks.test.ts",
    "test_strategy": "run existing coverage",
    "non_goals": [
      "do not refactor the entire hook system",
      "do not touch unrelated hook registration or validation",
      "do not change the public hook contract"
    ],
    "implementation_steps": [
      "Reproduce the timeout: pnpm vitest run tests/playbook-hooks.test.ts",
      "Inspect the hook execution path in packages/core/src/run.ts for unsettled promises or unhandled rejections when a hook throws",
      "Ensure thrown hook errors are caught and the rejection is resolved so downstream tasks can proceed",
      "Re-run the failing test to confirm it passes within the timeout"
    ],
    "acceptance_checks": [
      "pnpm --filter @converge/cli build",
      "pnpm --filter @converge/core build",
      "pnpm vitest run tests/playbook-hooks.test.ts",
      "pnpm vitest run tests/playbook-loop-seed.test.ts",
      "pnpm vitest run tests/playbook-seeds.test.ts"
    ],
    "selection_rank": 1,
    "risk": "medium",
    "rollback_plan": "Revert the run.ts hook-error-handling change. The fix is isolated to the hook execution promise chain.",
    "ledger_updates": {
      "backlog_items_to_add": [],
      "backlog_items_to_close": []
    }
  }
}
