{
  "epoch": "1",
  "selected": {
    "id": "invalid-model-config-errors",
    "priority_class": "api",
    "dimension": "API",
    "goal": "Add focused invalid provider/model configuration regression coverage so users get actionable early errors.",
    "why_now": "evidence: observe/findings.json identifies invalid-model-config-errors as the only high-severity maintainer finding after baseline build and playbook tests passed; this targets a critical API path, is not cosmetic cleanup, and no recent ledgers exist to indicate repeated low-value work.",
    "files": [
      "tests/mixed-model.test.ts",
      "packages/core/src/ai/factory.ts",
      "packages/claudefn/src/claudefn.ts"
    ],
    "test_command": "pnpm vitest run tests/mixed-model.test.ts",
    "test_strategy": "add failing regression first",
    "non_goals": [
      "Do not change the public model configuration schema beyond error behavior.",
      "Do not alter unrelated provider execution paths."
    ],
    "implementation_steps": [
      "Add a focused regression in tests/mixed-model.test.ts for invalid provider/model configuration.",
      "Tighten factory or claudefn error handling only as needed to produce a concise actionable error.",
      "Run the focused mixed-model test."
    ],
    "acceptance_checks": [
      "pnpm --filter @converge/core build",
      "pnpm vitest run tests/mixed-model.test.ts"
    ],
    "risk": "low",
    "rollback_plan": "Revert the regression and the small error-handling change together if the provider path behavior regresses.",
    "ledger_updates": {"backlog_items_to_add": [], "backlog_items_to_close": []}
  }
}
