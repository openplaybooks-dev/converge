{
  "epoch": "21",
  "selected": {
    "id": "select-parent-plus-spawned-coverage-recurrence",
    "priority_class": "determinism",
    "dimension": "Determinism",
    "goal": "Root-cause fix: --select parent+ fails to include dynamically spawned children in DAG selection — the spawned TASK.md is never materialized into the journal",
    "why_now": "evidence: tests/playbook-dag.test.ts:257 assertion fails — expected child-alpha TASK.md to exist in journal after --select parent+ but got false. This file appears in 3+ epochs (13, 19, 20) — per maintainer policy, a 3+ epoch hot-file requires a root-cause refactor, not another surface-level patch. The failure has persisted through multiple epochs because prior fixes were incremental (adding test coverage in epoch 13, compile fix in epoch 19, escalation in epoch 20) without addressing the underlying spawn-then-select materialization logic. Higher priorities checked: hooks timeout is a separate correctness bug (epoch 20 already escalated it), all other priority classes clean. Not a repeated low-value cleanup: this is a determinism root-cause fix on a long-failing test.",
    "files": [
      ".converge/playbooks/self-improvement-loop/scripts/check-selection-quality.mjs",
      ".converge/playbooks/self-improvement-loop/scripts/list-nonartifact-diff.mjs",
      ".converge/playbooks/self-improvement-loop/tasks/improve/seeds/epoch.seed.js",
      ".converge/playbooks/self-improvement-loop/templates/epoch/tasks/implement/TASK.md",
      ".converge/playbooks/self-improvement-loop/templates/epoch/tasks/observe/TASK.md",
      "packages/core/src/navigator/repair/strategies/seed-script-repair.ts",
      "packages/core/src/run.ts",
      "tests/playbook-dag.test.ts"
    ],
    "test_command": "pnpm vitest run tests/playbook-compile.test.ts tests/playbook-dag.test.ts tests/playbook-loop-seed.test.ts tests/playbook-seeds.test.ts",
    "test_strategy": "add failing regression first",
    "non_goals": ["do not change hook error handling", "do not touch CLI help or unrelated commands", "do not refactor unrelated DAG code"],
    "implementation_steps": [
      "Root-cause: trace why spawned child tasks are not materialized into the journal when --select parent+ is used",
      "Fix the spawn-then-select materialization gap in the DAG/run layer",
      "Add a focused regression test that verifies child-alpha TASK.md appears in journal after --select parent+",
      "Verify existing DAG and compile tests still pass"
    ],
    "acceptance_checks": [
      "pnpm --filter @converge/cli build",
      "pnpm --filter @converge/core build",
      "pnpm vitest run tests/playbook-compile.test.ts tests/playbook-dag.test.ts tests/playbook-loop-seed.test.ts tests/playbook-seeds.test.ts"
    ],
    "selection_rank": 2,
    "risk": "medium",
    "rollback_plan": "revert the spawn materialization change; the existing (failing) test documents the bug",
    "ledger_updates": {
      "backlog_items_to_add": [],
      "backlog_items_to_close": ["escalation: epoch 020 — DAG select+ failure (addressed by root-cause fix in epoch 021)"]
    }
  }
}
