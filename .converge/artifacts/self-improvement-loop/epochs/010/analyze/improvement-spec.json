{
  "epoch": "10",
  "selected": {
    "id": "hooks-throw-timeout",
    "priority_class": "correctness",
    "dimension": "Correctness",
    "goal": "Fix hook error handling so thrown errors do not stall or block downstream task execution",
    "why_now": "evidence: npx vitest run tests/playbook-hooks.test.ts → 'should handle hooks that throw without blocking downstream' times out at 10000ms. This is a priority-1 correctness bug (failing test with stalled execution). Higher priorities checked: no crashes or higher-severity failures observed — all other test suites pass clean. Last two epochs (004: escalation-duplicate-epochs, 005: runstate-missing-crash) are different failure classes within Correctness, so this is not a repeat. Hot-file rationale: packages/core/src/run.ts is touched in epochs 2, 003, 5 because it is the central execution orchestrator — each prior fix targeted a distinct subsystem (determinism, cache invalidation, runstate crash handling) within the same file, not the same bug. This epoch targets hook error handling, another distinct subsystem. A full refactor of run.ts into separate modules is tracked as a future backlog item; this fix is the smallest change that resolves the current stall.",
    "files": ["packages/core/src/run.ts", "tests/playbook-hooks.test.ts"],
    "test_command": "pnpm vitest run tests/playbook-loop-seed.test.ts tests/playbook-seeds.test.ts tests/playbook-hooks.test.ts",
    "test_strategy": "run existing coverage — the failing test already exists and reproduces the stall; fix the implementation so the test passes",
    "non_goals": ["do not rewrite the hooks system", "do not change hook registration API", "do not touch unrelated test files"],
    "implementation_steps": [
      "Read tests/playbook-hooks.test.ts around line 225 to understand the expected behavior when hooks throw",
      "Trace the hook execution path in packages/core/src/run.ts to find where a thrown error leaves an unresolved Promise",
      "Ensure the hook rejection is caught and propagated without stalling downstream task execution",
      "Run pnpm vitest run tests/playbook-hooks.test.ts to confirm the timeout is resolved and all 5 tests pass"
    ],
    "acceptance_checks": [
      "pnpm --filter @converge/cli build",
      "pnpm --filter @converge/core build",
      "pnpm vitest run tests/playbook-loop-seed.test.ts tests/playbook-seeds.test.ts tests/playbook-hooks.test.ts"
    ],
    "selection_rank": 1,
    "refactor_signal": "root-cause: run.ts is the central execution orchestrator touched in epochs 2, 003, 5 for distinct subsystems (determinism, cache invalidation, runstate crash). Each fix targeted a different code path. A full modular split is deferred to backlog.",
    "risk": "medium",
    "rollback_plan": "git revert the commit; the hook error path change is isolated to the run.ts hook execution logic",
    "ledger_updates": {
      "backlog_items_to_add": [],
      "backlog_items_to_close": []
    }
  }
}
