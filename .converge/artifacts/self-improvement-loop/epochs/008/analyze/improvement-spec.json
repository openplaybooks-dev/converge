{
  "epoch": "8",
  "selected": {
    "id": "vitest-missing-at-root",
    "priority_class": "production",
    "dimension": "Production Readiness",
    "goal": "Restore root-level vitest so documented test commands execute correctly",
    "why_now": "evidence: pnpm vitest run tests/playbook-compile.test.ts fails with 'vitest is not recognized' at project root. Root package.json devDependencies lacks vitest (only prettier and sass). All test_command entries in metrics.jsonl for epochs 1-5 reference 'pnpm vitest run tests/...' but none can currently execute. This is a production readiness blocker — no test regression can be verified before merging. Higher priorities checked: priority 1 (no failing framework tests/crashes in observation beyond this tooling gap), priority 2 (lifecycle correctness clean — no findings), priority 3 (DAG determinism clean — no findings). This is not a repeated or cosmetic change; the selected dimension ('Production Readiness') differs from the last two epochs ('Correctness'). No candidate file (package.json, vitest.config.ts) appears in any previous epoch's touched-files.jsonl.",
    "files": ["package.json", "vitest.config.ts"],
    "test_command": "pnpm vitest run tests/playbook-compile.test.ts tests/playbook-seeds.test.ts",
    "test_strategy": "run existing coverage",
    "non_goals": ["do not change test contents", "do not modify package-level vitest configs", "do not add new test files"],
    "implementation_steps": [
      "Add vitest to root devDependencies matching the version used in packages/core and packages/cli (vitest 4.1.4)",
      "Verify root vitest.config.ts resolves correctly with the new dependency",
      "Run pnpm install to hoist vitest to root",
      "Execute pnpm vitest run tests/playbook-compile.test.ts to confirm execution"
    ],
    "acceptance_checks": [
      "pnpm --filter @converge/cli build",
      "pnpm --filter @converge/core build",
      "pnpm vitest run tests/playbook-compile.test.ts"
    ],
    "selection_rank": 4,
    "risk": "low",
    "rollback_plan": "Remove vitest from root devDependencies and re-run pnpm install",
    "ledger_updates": {"backlog_items_to_add": [], "backlog_items_to_close": []}
  }
}
