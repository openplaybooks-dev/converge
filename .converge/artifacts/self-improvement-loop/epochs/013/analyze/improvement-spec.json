{
  "epoch": "13",
  "selected": {
    "id": "no-test-select-parent-plus",
    "priority_class": "determinism",
    "dimension": "Determinism",
    "goal": "Add a regression test verifying that --select parent+ includes dynamically spawned descendants in DAG selection",
    "why_now": "evidence: grep of tests/*.test.ts found zero tests exercising the parent+ select operator with dynamically spawned children. playbook-loop-seed.test.ts uses --select improve+ on a statically-known task; codex-real-runner.test.ts uses --select=hello (no + operator). This is a priority-3 (DAG determinism) gap under the maintainer policy. Higher ranks checked and clean: rank 1 (crash/stall) — all 136 tests pass, no crashes; rank 2 (state/lifecycle) — run-lock, cache-invalidation, and hooks tests all pass cleanly. The deprecation-warning-spam finding was rejected as same failure class as epoch 11 (deprecation dedup). Dimension (Determinism) and candidate file (tests/playbook-dag.test.ts) differ from both epoch 11 (Production Readiness, packages/core/src/run.ts) and epoch 12 (Correctness, packages/core/src/run.ts + tests/playbook-hooks.test.ts) — no anti-repeat violation.",
    "files": [
      "tests/playbook-dag.test.ts",
      "tests/test-select-parent-plus/.converge/playbooks/default/playbook.yml",
      "tests/test-select-parent-plus/.converge/playbooks/default/tasks/parent/TASK.md",
      "tests/test-select-parent-plus/.converge/playbooks/default/tasks/parent/seeds/spawn.seed.js",
      "tests/test-select-parent-plus/.converge/project.yaml"
    ],
    "test_command": "pnpm vitest run tests/playbook-compile.test.ts tests/playbook-dag.test.ts",
    "test_strategy": "add failing regression first",
    "non_goals": [
      "do not refactor the DAG selection engine itself",
      "do not touch --select parsing or CLI flag logic",
      "do not change the spawn/child materialization path"
    ],
    "implementation_steps": [
      "Create a test fixture playbook with a seed task that spawns dynamic children",
      "Add a test in tests/playbook-dag.test.ts that runs --select parent+ and asserts all spawned descendants are included",
      "Run the new test to confirm it fails (regression) or passes (coverage gap filled), then adjust to truly exercise the dynamic-descendant path"
    ],
    "acceptance_checks": [
      "pnpm --filter @converge/cli build",
      "pnpm --filter @converge/core build",
      "pnpm vitest run tests/playbook-compile.test.ts tests/playbook-dag.test.ts"
    ],
    "selection_rank": 3,
    "risk": "low",
    "rollback_plan": "Revert the test addition commit. No production code changed — only a new test file edit. Existing test suite is unaffected.",
    "ledger_updates": {
      "backlog_items_to_add": [],
      "backlog_items_to_close": []
    }
  }
}
