version: 2
name: converge

ai:
  default: claude
  providers:
    claude:
      provider: minimax
      env:
        ANTHROPIC_BASE_URL: https://api.minimax.io/anthropic
        ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}

variables: {}
plugins: []
