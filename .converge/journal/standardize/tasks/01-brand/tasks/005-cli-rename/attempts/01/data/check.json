{
  "taskId": "01-brand/005-cli-rename",
  "checks": [
    {
      "id": "no-harness-in-cli",
      "description": "No harness references in CLI source",
      "cmd": "test -z \"$(grep -i 'harness' packages/core/src/cli/*.ts 2>/dev/null | grep -v auto-verify | grep -v '.converge/')\""
    }
  ]
}