# FEEDBACK.md — Check Results

**Status**: ❌ 1/1 check(s) failed

- ❌ **preview-deployed**

## ❌ preview-deployed

**Command**: `test -f apps/landing/.preview-deploy-url && grep -qE 'pages\.dev|workers\.dev' apps/landing/.preview-deploy-url`
**Exit code**: 1
**Output**:
```
Command failed: test -f apps/landing/.preview-deploy-url && grep -qE 'pages\.dev|workers\.dev' apps/landing/.preview-deploy-url
```
