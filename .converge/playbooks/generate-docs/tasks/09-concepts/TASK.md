---
id: 09-concepts
title: Phase 09 — Concepts (3 mental-model pages)
blocking: true
dependencies: [03-ia]
outputs:
  - docs/concepts/gap-driven-model.md
  - docs/concepts/filesystem-as-plan.md
  - docs/concepts/self-correction.md
---

The conceptual layer. Reader has used Converge; these pages explain *why*
it's shaped the way it is. Three mental-model pages, ~600-1000 words each.

These pages are reached primarily *from* problem-shaped guides — every
guide ends with a "for the why, see /concepts/<page>" pointer. This
phase therefore stays as-is.

1. **001-gap-driven-model** — the central paradigm. Target state, gaps, convergence loop.
2. **002-filesystem-as-plan** — why everything is files. Debuggability, git diff, no opaque state.
3. **003-self-correction** — LEARN.md as feedback loop. Failure-as-context, not retry-and-hope.
