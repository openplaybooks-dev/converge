name: nested-loop
description: '2-level nested incremental spawning.

  The outer parent spawns batches, and each batch incrementally spawns items.

  '
run:
  maxTaskAttempts: 10
  maxDuration: 2m
  maxStallCount: 10
