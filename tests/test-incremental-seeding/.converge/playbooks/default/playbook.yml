name: default
description: |
  Incremental CLI spawning — do-while pattern.
  The parent emits one child per iteration.
  Uses materialization: "incremental" so the seed re-runs
  each DAG pass until it returns false.
tasks:
  - id: parent
