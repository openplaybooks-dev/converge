name: default
description: |
  Incremental spawning — do-while pattern.
  A passthrough parent emits one child per wave and halts after 3 waves.
tasks:
  - id: parent
run:
  maxTaskAttempts: 10
  maxDuration: 2m
  maxStallCount: 10
