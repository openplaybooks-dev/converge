name: for-each
description: |
  Incremental spawning — for-each pattern.
  A passthrough parent walks a fixed item list and emits one item child per wave.
tasks:
  - id: process-all
run:
  maxTaskAttempts: 10
  maxDuration: 2m
  maxStallCount: 10
