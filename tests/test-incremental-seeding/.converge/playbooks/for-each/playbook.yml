name: for-each
description: |
  Incremental CLI spawning — for-each pattern.
  The parent processes a fixed list of items, emitting one
  child per iteration. Checks seed.json to find already-spawned
  children. Returns false when the list is exhausted.
tasks:
  - id: process-all
