# Human Review Playbook Fixture

Static fixture used by `tests/human-review-artifact.test.ts`.

It exists so the framework-level HTML review artifact test can copy a real
published playbook into a temporary workspace instead of inventing one at
runtime.
