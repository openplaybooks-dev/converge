name: default
description: Simplest possible playbook — creates a file and verifies it exists

run:
  mode: autonomous
  maxTaskAttempts: 3

tasks:
  - id: create-file

checks:
  - id: output-exists
    cmd: test -f output.txt
