name: hello-world
description: Simplest possible Converge playbook — two static tasks producing two output files

ai:
  default: claude
  providers:
    # Claude CLI — default Anthropic endpoint. Set ANTHROPIC_API_KEY before running.
    claude:
      provider: claude
