name: for-each
description: |
  Incremental seeding — for-each pattern.
  The seed function processes a fixed list of items, spawning one
  child per iteration. Checks seed.json to find already-spawned
  children. Returns false when the list is exhausted.
tasks:
  - id: process-all
