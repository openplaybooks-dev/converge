{
  "$schema": "v1",
  "game_type": "side-scroller",
  "confidence": 1.0,
  "reasoning": "Side-scroller per MODERN_SIDE_SCROLL_SPEC.md and the tokens library under assets/tokens/ (parallax bg-far/bg-mid/play/fg layers, gravity-based platforms, gameplay-on-play-only tokens).",
  "view": "side-2d",
  "movement": ["left-right", "jump"],
  "art_style_keywords": ["painterly", "Hollow Knight atmospheric", "Ori-style depth", "parallax", "saturated near + desaturated far"],
  "anti_keywords": ["top-down", "8-direction", "tile-based RPG", "16-bit pixel art", "photorealistic"],

  "camera": {
    "view": "side-scroller-perspective",
    "tilt_deg": 30,
    "forward_offset_tiles": 2,
    "description": "Side-scroller camera tilted ~30° down from horizontal (~120° from straight-up) and pushed slightly forward of the subject. Every painted concept (token, landscape, hero-shot, style sheet) reads with a hint of dimensionality from this angle: a visible top surface PLUS a visible side face. Grounds and platforms show a top surface with a body falling away below it (cartoon-platformer chunk of land, NOT a thin cross-section strip). Trees, rocks, pickups all sit at this slight 3/4-perspective angle. This camera is the binding visual contract for the project — every generator must honor it for cross-asset consistency."
  },

  "biomes": ["grassland"],

  "asset_categories": {
    "characters": {
      "enabled": true,
      "frames_mode": "video",
      "default_states": ["idle", "walk", "jump", "fall"]
    },
    "shared_props": {
      "enabled": true,
      "categories": ["item", "weapon", "interactive"]
    },
    "tilemap": {
      "enabled": false,
      "_note": "Side-scrollers don't use an 8-direction RPG tilemap. The per-biome design tokens under assets/tokens/{biome}/ are the building blocks. This section is reserved for top-down games."
    },
    "background": {
      "enabled": true,
      "layers": ["bg-far", "bg-mid", "fg"],
      "_note": "Backgrounds are painted per-scene from the per-layer maps stamped from token instances. See MODERN_SIDE_SCROLL_SPEC.md §5."
    },
    "scene_props": {
      "enabled": true,
      "categories": ["interactive", "decoration"]
    }
  },

  "playbook_overrides": {
    "scenes_inspect_use_platforms": true,
    "scenes_inspect_view_mode": "side-2d",
    "world_width_tiles": 120,
    "world_height_tiles": 12
  }
}
