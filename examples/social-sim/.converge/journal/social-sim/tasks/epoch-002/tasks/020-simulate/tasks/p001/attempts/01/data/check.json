{
  "taskId": "epoch-002/020-simulate/p001",
  "checks": [
    {
      "id": "action-appended",
      "description": "Exactly one timeline row exists for tick=2 personaId=p001",
      "cmd": "python3 -c \"import json,sys; lines=[json.loads(l) for l in open('runs/run-2026-04-25T01-45/timeline.jsonl') if l.strip()]; hits=[l for l in lines if l.get('tick')==2 and l.get('personaId')=='p001']; sys.exit(0 if len(hits)==1 else 1)\"\n"
    },
    {
      "id": "action-valid",
      "description": "Recorded action is one of post/repost/reply/like/follow/nothing",
      "cmd": "python3 -c \"import json,sys; lines=[json.loads(l) for l in open('runs/run-2026-04-25T01-45/timeline.jsonl') if l.strip()]; hits=[l for l in lines if l.get('tick')==2 and l.get('personaId')=='p001']; r=hits[0] if hits else {}; sys.exit(0 if r.get('action') in ['post','repost','reply','like','follow','nothing'] else 1)\"\n"
    },
    {
      "id": "vault-action-note",
      "description": "Vault action note exists for this tick × persona",
      "cmd": "ls vault/runs/run-2026-04-25T01-45/actions/t2-p001-*.md 2>/dev/null | head -1 | grep -q .\n"
    }
  ]
}