---
name: default
description: Fullstack app with Seed-driven task decomposition
run:
  mode: oneoff
  maxIterations: 50
  maxTaskAttempts: 3
goals:
  - id: has-frontend
    description: has-frontend
    checks:
      - id: check
        cmd: test -f src/app.ts
  - id: has-backend
    description: has-backend
    checks:
      - id: check
        cmd: test -f src/server.ts
