# React Managing State Skill

Implement only the state the app actually needs.

## Typical Responsibilities

- preferences and theme state
- filters and search
- playful interaction state
- mock data selection and derived views

## Rules

- prefer Zustand or plain React context/hooks
- keep state local when possible
- persist only user-facing preferences that matter across reloads
- never add speculative server-state abstractions

