# React Theming Apps Skill

Implement theme primitives for a React app using CSS variables and semantic naming.

## Output Targets

- `src/theme/theme.css`
- root theme hooks or utilities if needed

## Requirements

- light and dark themes
- semantic variables, not raw color literals everywhere
- one persisted theme or preference path using local storage
- image-aware surfaces so text remains readable over rich backgrounds

