version: 2
name: examples

# AI provider configuration — one default, multiple enabled.
# Replace ${ENV_VAR} placeholders with real keys or export them in your shell.
ai:
  default: claude
  providers:
    claude:
      provider: claude
      # Uses Anthropic's Claude CLI. Auth via ANTHROPIC_AUTH_TOKEN env var.

variables: {}
plugins: []
