name: deep-research
description: Linear 6-task research pipeline (initial survey → deep dive → final report). One folder under questions/ per research question.

run:
  maxIterations: 30
  maxTaskAttempts: 3
  maxDuration: 12h
  resume: true
