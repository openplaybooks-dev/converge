name: default
description: Sequential data pipeline with task dependencies

run:
  mode: oneoff
  maxTaskAttempts: 3

tasks:
  - path: fetch-data
  - path: transform
  - path: validate

checks:
  - id: pipeline-complete
    cmd: test -f data/validated.json
